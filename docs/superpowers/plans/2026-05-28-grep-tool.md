# Grep Tool for AI Agent — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 AI Agent 新增 `grep` tool，使用 ripgrep 正则搜索工作目录文件内容。

**Architecture:** 新建 `cortex/grep_tools.py`，定义 Anthropic tool schema + handler。handler 调用 `treesearch.ripgrep.rg_search()` 获取命中行号，再读取文件提取匹配行文本，格式化为 `path:line: text` 输出。在 `agent_integration.py` 中注册。

**Tech Stack:** ripgrep (subprocess), pathlib, Anthropic tool use schema

---

## File Structure

| 文件 | 责任 |
|------|------|
| `cortex/grep_tools.py` | 新建。GREP_TOOL schema + `build_grep_tools()` 入口 + `_handle_grep()` handler + `_read_matched_lines()` 辅助函数 |
| `cortex/agent_integration.py:227-228` | 修改。在 kb_tools 注册之后注册 grep tool（~3 行） |

---

### Task 1: 创建 `grep_tools.py` — schema + 入口函数

**Files:**
- Create: `cortex/grep_tools.py`

- [ ] **Step 1: 创建文件，写入 schema + build_grep_tools 入口**

```python
"""grep 工具定义和处理器

为 AI Agent 提供 ripgrep 正则搜索能力，搜索工作目录下所有文件。
"""

import logging
import os
from pathlib import Path
from typing import Callable, Dict, List, Tuple

logger = logging.getLogger(__name__)

# ---------------------------------------------------------------------------
# 常量
# ---------------------------------------------------------------------------

MAX_TOTAL_CHARS = 8000
DEFAULT_MAX_RESULTS = 50

# ---------------------------------------------------------------------------
# Anthropic tool use schema
# ---------------------------------------------------------------------------

GREP_TOOL = {
    "name": "grep",
    "description": (
        "在工作目录中使用正则表达式搜索文件内容。"
        "支持 ripgrep 正则语法，搜索所有文件（包括未索引的）。"
        "当 search_kb 未找到结果，或需要精确匹配代码/配置中的特定模式时使用。"
    ),
    "input_schema": {
        "type": "object",
        "properties": {
            "pattern": {
                "type": "string",
                "description": "正则表达式搜索模式（ripgrep 语法）",
            },
            "glob": {
                "type": "string",
                "description": "文件过滤 glob 模式，如 '*.py', '*.{md,txt}'",
            },
            "case_sensitive": {
                "type": "boolean",
                "description": "是否区分大小写，默认 false",
                "default": False,
            },
            "max_results": {
                "type": "integer",
                "description": "最大返回结果数，默认 50",
                "default": 50,
            },
        },
        "required": ["pattern"],
    },
}

# ---------------------------------------------------------------------------
# 公共入口
# ---------------------------------------------------------------------------

def build_grep_tools(
    workdir: Path,
) -> Tuple[List[Dict], Dict[str, Callable]]:
    """构建 grep 工具定义和处理器。

    Args:
        workdir: 工作目录（搜索根路径）

    Returns:
        (tools, handlers) 元组
    """
    handlers = {
        "grep": lambda **kw: _handle_grep(workdir, **kw),
    }
    return [GREP_TOOL], handlers
```

- [ ] **Step 2: 验证文件可被导入**

Run: `cd /c/Users/lianghao/github/cortex && .venv/Scripts/python.exe -c "from cortex.grep_tools import build_grep_tools, GREP_TOOL; print('OK:', GREP_TOOL['name'])"`
Expected: `OK: grep`

---

### Task 2: 实现 `_read_matched_lines` 辅助函数

**Files:**
- Modify: `cortex/grep_tools.py`

此函数负责从 `rg_search` 返回的 `{path: [line_nums]}` 中读取每个匹配行的文本内容。

- [ ] **Step 1: 在 `build_grep_tools` 之后添加辅助函数**

```python
# ---------------------------------------------------------------------------
# 辅助函数
# ---------------------------------------------------------------------------

def _read_matched_lines(
    hits: dict[str, list[int]],
    workdir: Path,
    max_results: int,
) -> list[tuple[str, int, str]]:
    """从 rg_search 结果中读取匹配行内容。

    Args:
        hits: {file_path: [1-based line_numbers]}
        workdir: 工作目录（用于生成相对路径）
        max_results: 最大返回条数

    Returns:
        [(relative_path, line_number, line_text)]
    """
    results: list[tuple[str, int, str]] = 0  # sentinel — see below
    results = []
    total = 0

    for abs_path, line_nums in hits.items():
        if total >= max_results:
            break

        # 生成相对路径用于展示
        try:
            rel_path = os.path.relpath(abs_path, str(workdir))
        except ValueError:
            rel_path = abs_path

        try:
            with open(abs_path, "r", encoding="utf-8", errors="replace") as f:
                all_lines = f.readlines()
        except OSError:
            continue

        for line_num in line_nums:
            if total >= max_results:
                break
            idx = line_num - 1  # 转为 0-based
            if 0 <= idx < len(all_lines):
                line_text = all_lines[idx].rstrip("\n\r")
                results.append((rel_path, line_num, line_text))
                total += 1

    return results
```

> **注意：** 删除 `results: list[...] = 0` 那行（sentinel 注释），只保留 `results = []`。上面代码块中的写法是为了演示，实际写入时只保留：
> ```python
>     results: list[tuple[str, int, str]] = []
> ```

- [ ] **Step 2: 验证可导入**

Run: `cd /c/Users/lianghao/github/cortex && .venv/Scripts/python.exe -c "from cortex.grep_tools import _read_matched_lines; print('OK')"`
Expected: `OK`

---

### Task 3: 实现 `_handle_grep` handler

**Files:**
- Modify: `cortex/grep_tools.py`

- [ ] **Step 1: 在 `_read_matched_lines` 之后添加 handler 函数**

```python
# ---------------------------------------------------------------------------
# 处理函数
# ---------------------------------------------------------------------------

def _handle_grep(
    workdir: Path,
    *,
    pattern: str,
    glob: str | None = None,
    case_sensitive: bool = False,
    max_results: int = DEFAULT_MAX_RESULTS,
) -> str:
    """在工作目录中搜索文件内容。"""
    from treesearch.ripgrep import rg_available, rg_search

    if not rg_available():
        return "ripgrep (rg) 未安装或不在 PATH 中，无法使用 grep 工具。"

    if not pattern:
        return "搜索模式不能为空。"

    # 构造搜索路径
    if glob:
        file_paths = [str(p) for p in Path(workdir).rglob(glob) if p.is_file()]
        if not file_paths:
            return f"未找到匹配 glob '{glob}' 的文件。"
    else:
        file_paths = [str(workdir)]

    # 执行 ripgrep 搜索
    hits = rg_search(
        pattern,
        file_paths,
        case_sensitive=case_sensitive,
        use_regex=True,
        max_count=max_results,
    )

    if not hits:
        return f"未找到匹配 '{pattern}' 的结果。"

    # 读取匹配行内容
    matched_lines = _read_matched_lines(hits, workdir, max_results)

    if not matched_lines:
        return f"未找到匹配 '{pattern}' 的结果。"

    # 统计文件数
    unique_files = len({p for p, _, _ in matched_lines})
    total_hits = sum(len(nums) for nums in hits.values())

    # 格式化输出
    lines = [f"Found {total_hits} results in {unique_files} files:\n"]
    total_chars = len(lines[0])
    shown = 0

    for rel_path, line_num, text in matched_lines:
        entry = f"{rel_path}:{line_num}: {text}"
        entry_len = len(entry) + 1  # +1 for newline

        if total_chars + entry_len > MAX_TOTAL_CHARS:
            remaining = total_hits - shown
            if remaining > 0:
                lines.append(f"\n({remaining} more results truncated. Use max_results parameter to get more.)")
            break

        lines.append(entry)
        total_chars += entry_len
        shown += 1

    if shown == len(matched_lines) and total_hits > shown:
        remaining = total_hits - shown
        lines.append(f"\n({remaining} more results truncated. Use max_results parameter to get more.)")

    return "\n".join(lines)
```

- [ ] **Step 2: 验证完整模块可导入**

Run: `cd /c/Users/lianghao/github/cortex && .venv/Scripts/python.exe -c "from cortex.grep_tools import build_grep_tools; tools, handlers = build_grep_tools(Path('.')); print('tools:', [t['name'] for t in tools]); print('handlers:', list(handlers.keys()))"`
Expected: `tools: ['grep']` 和 `handlers: ['grep']`

---

### Task 4: 集成注册到 `agent_integration.py`

**Files:**
- Modify: `cortex/agent_integration.py:227-228`

- [ ] **Step 1: 在 kb_tools 注册之后添加 grep tool 注册**

在 `cortex/agent_integration.py` 第 228 行（`register_external_tools(kb_tools, kb_handlers)` 之后）添加：

```python
        # --- grep 工具注册 ---
        from cortex.grep_tools import build_grep_tools
        grep_tools, grep_handlers = build_grep_tools(self.workdir)
        register_external_tools(grep_tools, grep_handlers)
```

即插入到现有第 228 行和第 230 行之间，将原来的：

```python
        from planify.tools.registry import register_external_tools
        register_external_tools(kb_tools, kb_handlers)

        # 部署技能文件到 ~/.cortex/skills/
```

改为：

```python
        from planify.tools.registry import register_external_tools
        register_external_tools(kb_tools, kb_handlers)

        # --- grep 工具注册 ---
        from cortex.grep_tools import build_grep_tools
        grep_tools, grep_handlers = build_grep_tools(self.workdir)
        register_external_tools(grep_tools, grep_handlers)

        # 部署技能文件到 ~/.cortex/skills/
```

注意：`register_external_tools` 已在第 227 行导入，无需重复导入。

- [ ] **Step 2: 验证 agent_integration.py 可导入**

Run: `cd /c/Users/lianghao/github/cortex && .venv/Scripts/python.exe -c "import cortex.agent_integration; print('OK')"`
Expected: `OK`

---

### Task 5: 端到端验证

- [ ] **Step 1: 验证 grep tool 在测试工作目录中正常工作**

Run: `cd /c/Users/lianghao/github/cortex/test_work_dir && ../.venv/Scripts/python.exe -c "
from pathlib import Path
from cortex.grep_tools import build_grep_tools

workdir = Path('.')
tools, handlers = build_grep_tools(workdir)

# 测试 grep handler
result = handlers['grep'](pattern='def ', glob='*.py', max_results=5)
print(result[:500])
print('---')
print('Tool names:', [t['name'] for t in tools])
"`
Expected: 看到类似 `Found N results in M files:` 的输出，包含 `path:line: text` 格式的匹配行。

- [ ] **Step 2: 验证无结果时的输出**

Run: `cd /c/Users/lianghao/github/cortex/test_work_dir && ../.venv/Scripts/python.exe -c "
from pathlib import Path
from cortex.grep_tools import build_grep_tools

workdir = Path('.')
tools, handlers = build_grep_tools(workdir)
result = handlers['grep'](pattern='ZZZZZZZ_NOT_EXIST_12345')
print(result)
"`
Expected: `未找到匹配 'ZZZZZZZ_NOT_EXIST_12345' 的结果。`

- [ ] **Step 3: 验证 glob 过滤**

Run: `cd /c/Users/lianghao/github/cortex/test_work_dir && ../.venv/Scripts/python.exe -c "
from pathlib import Path
from cortex.grep_tools import build_grep_tools

workdir = Path('.')
tools, handlers = build_grep_tools(workdir)
result = handlers['grep'](pattern='.', glob='*.md', max_results=3)
print(result[:500])
"`
Expected: 只包含 `.md` 文件的搜索结果。

- [ ] **Step 4: Commit**

```bash
git add cortex/grep_tools.py cortex/agent_integration.py docs/superpowers/
git commit -m "feat(agent): 新增 grep tool，支持 ripgrep 正则搜索工作目录"
```
